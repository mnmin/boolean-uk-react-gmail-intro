import Button from "./Button";
const EmailActions = () => {
  return (
    <section className="email-actions">
      <Button text="reply" />
      <Button text="forward" />
    </section>
  );
};
export default EmailActions;
